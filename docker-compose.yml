# Agent Ethos Backend - Docker Compose
# For local development

version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./agent_ethos.db
      - CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
      - ENVIRONMENT=development
      - SECRET_KEY=dev-secret-key-change-in-production
    volumes:
      # Mount for development (hot reload)
      - .:/app
      # Persist SQLite database
      - ./data:/app/data
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: PostgreSQL for production-like local testing
  # Uncomment to use PostgreSQL instead of SQLite
  # 
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: ethos
  #     POSTGRES_PASSWORD: ethos_dev_password
  #     POSTGRES_DB: agent_ethos
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U ethos -d agent_ethos"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:

